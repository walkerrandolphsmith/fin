# @fin/domain

`@fin/domain` contains the core business model and rules. This layer of abstraction should be a direct reflection of the problem being modeled and use ubiquitous language. It has no dependencies on infrastructure, persistence layer, or web frameworks. Everything here should be pure domain logic

- entities
- value objects
- domain services
- events
- invariants
